<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Eliminar Curso Producción Agraria</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/ae360af17e.js" crossorigin="anonymous"></script>
    <style>
        body {
            background-color: #d3f1c0;
        }

        .container {
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
            border-radius: 10px;
            max-width: 600px;
            margin-top: 20px;
            margin-bottom: 20px;
        }

        h2 {
            color: #4caf50;
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            color: #4caf50;
            display: flex;
            align-items: center;
            margin-bottom: 5px;
        }

        label i {
            width: 20px;
            text-align: center;
            margin-right: 10px;
        }

        .form-control {
            margin-bottom: 15px;
        }

        .btn-danger {
            background-color: #f44336;
            border-color: #f44336;
        }

        .btn-danger:hover {
            background-color: #d32f2f;
            border-color: #d32f2f;
        }
    </style>
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container">
        <h2 class="text-center"><i class="fas fa-leaf" style="color: #4caf50;"></i> Eliminar Curso Producción Agraria</h2>
        <form action="/cursos_produccion/eliminar/<%= courseId %>" method="post">
            <!-- Muestra la información del curso a eliminar -->
            <p>¿Estás seguro de que deseas eliminar el curso "<%= curso[0] %>"?</p>
            <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i> Confirmar Eliminación</button>
        </form>
        <a href="/cursos_produccion" class="btn btn-secondary"><i class="fas fa-arrow-left"></i> Volver</a>
    </div>
</body>

</html>
